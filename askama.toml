[general]
# Directories to search for templates, relative to the crate root.
dirs = [".//target//templates"]
# Unless you add a `-` in a block, whitespace characters won't be trimmed.
whitespace = "preserve"